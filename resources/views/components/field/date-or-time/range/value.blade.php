<span class="dms-date-or-time-range-display" data-mode="{{ $mode }}" data-date-format="{{ $format }}">
    <span class="dms-start-display">{{ $value['start'] }}</span>
    to
    <span class="dms-end-display">{{ $value['end'] }}</span>
</span>